[project]
name = "arbos"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "hyperliquid-python-sdk",
    "requests",
    "python-dotenv",
    "pyTelegramBotAPI",
]

[tool.setuptools]
py-modules = ["arbos"]
